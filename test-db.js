import { initializeApp } from "firebase/app"; import { getFirestore, collection, getDocs } from "firebase/firestore"; import fs from "fs"; const firebaseConfig = JSON.parse(fs.readFileSync("./firebase-applet-config.json", "utf-8")); const app = initializeApp(firebaseConfig); const db = getFirestore(app); async function testDB() { try { const snap = await getDocs(collection(db, "products")); console.log("Products count:", snap.size); snap.forEach(d => { const data = d.data(); console.log("-", data.id, data.name, "Has Image?", !!data.image); }); } catch(e) { console.error(e); } } testDB();
